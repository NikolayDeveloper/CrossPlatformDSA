ѕримечание, если что то не работает запросите новый sdk from pki.gov.kz и обновите библиотеки  ------ это пр€м важно!


ƒл€ Linux

-----ѕервый вариант ------
Ќужно определить дефолтную дирректорию дл€ хранени€ динамических библиотек операционной системы в Ubuntu она в /usr/lib, это нужно чтобы не определ€ть переменную окружени€ дл€ поиска библиотек в конкретной дирректории
1a) из 'Linux/C' скопировать libkalkancryptwr-64.so и libkalkancryptwr-64.so.1.1.1 в /usr/lib/
из 'Linux/libs_for_linux' тоже копируем все библиотеки в /usr/lib/ причем списком без директорий.
---------------------
-----¬торой вариант ------
1b) Ќо можно все же определить переменную окружени€ дл€ этого пройти в Linux\libs_for_linux и скопировать папку kalkancrypt в каталог: /opt/
	
	sudo cp -r kalkancrypt /opt/

«атем необходимо добавить в переменную окружени€ LD_LIBRARY_PATH путь к папке с библиотеками. 
	
	export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/opt/kalkancrypt/:/opt/kalkancrypt/lib/engines


--------------------------

ƒл€ проверки, что у libkalkancryptwr-64.so все впор€дке с зависимост€ми используй эту команду   "ldd /usr/lib/libkalkancryptwr-64.so" , если там нету типа Not found чего то, то все в пор€дке


2) открываем 'Linux/ca-certs/.../readme.txt' и читаем


ƒл€ Windows
 устанавливаем корневые сертификаты из pki.gov.kz